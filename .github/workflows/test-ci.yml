name: Test with Docker CI

on:
  push:
    branches: [ feat/add-test-action ]

jobs:
  integration-test-with-docker:
    runs-on: ubuntu-latest

    services:
      db:
        image: mariadb:10
        env:
          MYSQL_DATABASE: fosslight
          MYSQL_PASSWORD: fosslight
          MYSQL_USER: fosslight
          MYSQL_ROOT_PASSWORD: fosslight
        ports:
          - 3306:3306
        options: -v /usr/bin/docker/db/conf.d:/etc/mysql/conf.d
                 -v /usr/bin/docker/db/data:/var/lib/mysql
                 -v /usr/bin/docker/db/initdb.d:/docker-entrypoint-initdb.d
                 --name="fosslight_db"
                 --health-cmd="mysqladmin ping"
                 --health-interval=10s
                 --health-timeout=5s
                 --health-retries=3

    steps:
      # - name: Give permission
      #   run: sudo chown -R $USER:$USER /home/runner/work/fosslight/fosslight

      - uses: actions/checkout@v2

      - name: Check if init.sql exists in db container
        run: docker exec fosslight_db ls -la ./docker-entrypoint-initdb.d/

      - name: Check DB schema
        run: docker exec -i fosslight_db mysql -ufosslight -pfosslight -e "show databases; use fosslight; show tables;"

      - name: Check running containers
        run: docker ps